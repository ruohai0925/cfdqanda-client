# CFDQandA 用户指南

> CFDQandA 是一个基于大语言模型（LLM）的 CFD 仿真自动化平台。你只需用自然语言描述仿真需求，平台会自动完成 OpenFOAM 的网格划分、边界条件设置、求解器运行和后处理。

---

## 目录

1. [注册与登录](#1-注册与登录)
2. [界面概览](#2-界面概览)
3. [创建仿真任务](#3-创建仿真任务)
4. [模型选择](#4-模型选择)
5. [执行模式](#5-执行模式)
6. [任务状态说明](#6-任务状态说明)
7. [查看仿真结果](#7-查看仿真结果)
8. [提供反馈](#8-提供反馈)
9. [下载数据](#9-下载数据)
10. [使用限制与配额](#10-使用限制与配额)
11. [常见问题](#11-常见问题)
12. [隐私政策](#12-隐私政策)

---

## 1. 注册与登录

### 1.1 注册新账户

注册需要以下信息：

| 字段 | 是否必填 | 说明 |
|------|---------|------|
| 邮箱 | 必填 | 用于登录和接收验证邮件 |
| 密码 | 必填 | 自行设置 |
| 姓名 | 必填 | 显示在系统中的用户名 |
| 机构 | 选填 | 你所在的大学或公司 |
| 邀请码 | 必填 | 格式为 `CFDQ-XXXX-XXXX-XXXX`，需向管理员索取 |

注册流程：

1. 在登录页面点击「注册」切换到注册表单
2. 依次填写邮箱、密码、姓名、机构
3. 输入管理员提供的邀请码（系统会自动转为大写）
4. 勾选隐私政策同意框
5. 点击「注册」按钮
6. 前往邮箱点击验证链接，完成注册

> **注意**：注册后系统会发送一封验证邮件到你填写的邮箱。请点击邮件中的链接完成验证，否则无法登录。如果没有收到邮件，请检查垃圾邮件文件夹，或联系管理员。

> **注意**：每个邀请码只能使用一次，且绑定一个邮箱。如果注册过程中遇到问题，请联系管理员重置邀请码。

### 1.2 登录

使用注册时的邮箱和密码直接登录即可。

---

## 2. 界面概览

登录后，你会看到主界面包含以下区域：

- **顶部**：语言切换（中/英）、用户信息、登出按钮
- **上半部分**：任务创建区（Prompt 输入框、模型选择、执行设置）
- **下半部分**：历史记录列表（按时间倒序排列）

---

## 3. 创建仿真任务

### 3.1 编写 Prompt

在输入框中用自然语言描述你的仿真需求。一个好的 Prompt 应包含：

- **流体类型**：不可压缩/可压缩、牛顿/非牛顿流体
- **几何形状**：计算域的尺寸和形状
- **边界条件**：入口速度、出口压力、壁面条件等
- **求解器偏好**：如有特定要求（icoFoam、simpleFoam 等）
- **湍流模型**：层流 / k-epsilon / k-omega SST / Spalart-Allmaras 等

### 3.2 示例 Prompt

平台提供了 4 个预设示例，点击即可自动填入：

| 示例 | 类型 | 求解器 | 难度 |
|------|------|--------|------|
| **顶盖驱动方腔流** | 不可压缩 | icoFoam | 入门 |
| **后向台阶流** | 不可压缩 | simpleFoam + k-epsilon | 湍流 |
| **二维翼型** | 不可压缩 | simpleFoam + Spalart-Allmaras | 外气动 |
| **自然对流** | 传热 | buoyantFoam | 传热 |

### 3.3 提交

填写 Prompt 后点击「提交任务」。系统会立即返回确认，任务进入排队状态。

---

## 4. 模型选择

点击「模型选择」展开设置面板。平台提供三种模型选项：

### 4.1 Codex gpt-5.3-codex（默认）

- **费用**：平台承担，免费使用
- **限制**：每人每天 10 次
- **适合**：日常测试、快速验证

### 4.2 Codex (gpt-5.3-codex)

- **费用**：平台承担，免费使用
- **限制**：全平台共享每日额度，先到先得
- **质量**：旗舰级 coding 模型，成功率更高
- **可选**：如果你有自己的 Codex Token，可以填入以不受平台额度限制
- **适合**：对仿真质量要求高的任务

### 4.3 自带 API Key（BYOK）

使用你自己的 API Key，不受平台额度限制。支持以下 LLM 提供商：

| 提供商 | 可选模型 | API Key 格式 |
|--------|---------|-------------|
| **OpenAI** | gpt-4o, gpt-4o-mini, gpt-4.1, gpt-4.1-mini, gpt-5-mini, o3, o4-mini | 以 `sk-` 开头 |
| **Anthropic** | claude-sonnet-4-5, claude-opus-4-6, claude-haiku-4-5 | 以 `sk-ant-` 开头 |
| **DeepSeek** | deepseek-chat (V3), deepseek-reasoner (R1) | 任意格式 |
| **通义千问** | qwen-plus, qwen-turbo, qwen-max | 任意格式 |

> **隐私保障**：你的 API Key 仅用于当前任务。Worker 读取后会**立即从数据库中删除**，不会被存储或传给第三方。

---

## 5. 执行模式

点击「执行设置」展开高级选项。

### 5.1 端到端模式（End-to-End）— 默认

全自动执行：提交 Prompt → 系统自动完成所有步骤 → 返回最终结果。

适合：
- 对 LLM 生成质量有信心
- 批量测试
- 不需要中间检查

### 5.2 交互模式（Interactive）

分步执行，可在关键节点暂停，等你确认后再继续。

**可选检查点：**

| 检查点 | 说明 | 暂停时机 |
|--------|------|---------|
| **检查生成文件** | 查看 LLM 生成的 OpenFOAM 配置文件（controlDict, fvSchemes, boundary 等） | 文件生成后、运行前 |
| **检查 Pre-Run 结果** | 查看短时间仿真的残差趋势和初始场 | Pre-Run 完成后、完整仿真前 |

**Pre-Run 步数设置**（仅交互模式）：

| 选项 | 说明 |
|------|------|
| 单步验证（默认） | 只运行 1 个时间步，快速检查配置是否正确 |
| 10 步 | 运行 10 个时间步，查看初始残差趋势 |
| 100 步 | 运行 100 个时间步，更充分地验证收敛性 |

**交互流程：**

```
提交任务 → 系统生成文件 → [检查点1: 查看文件]
    → 你点击「继续运行」→ 系统 Pre-Run → [检查点2: 查看 Pre-Run 结果]
    → 你点击「继续运行」→ 完整仿真 → 完成
```

在每个检查点，你可以：
- **继续运行**：确认当前阶段结果没问题，继续下一步
- **放弃**：终止任务（标记为失败）

---

## 6. 任务状态说明

提交后，任务会经历以下状态变化：

| 状态 | 含义 | 你需要做什么 |
|------|------|-------------|
| **queued** | 在队列中等待 | 等待即可。会显示排队位置（如"排队第 2 位"）和等待时长 |
| **running** | 正在执行仿真 | 等待即可 |
| **checkpoint** | 暂停等待你确认（仅交互模式） | 查看中间结果，点击「继续运行」或「放弃」 |
| **completed** | 仿真完成 | 查看结果、下载文件、提供反馈 |
| **failed** | 仿真失败 | 查看错误原因（显示在任务卡片中） |
| **cancelled** | 被你手动取消 | 可以查看已生成的部分文件 |

### 状态筛选

历史记录上方有筛选标签：**全部** | **进行中** | **已完成** | **已取消** | **失败**

### 常见错误原因

任务失败时，系统会在任务卡片中显示具体原因：

| 错误类型 | 提示信息 | 建议 |
|---------|---------|------|
| Codex 额度用完 | "平台 Codex 共享额度已用完" | 稍后重试或切换模型 |
| API 限速 | "LLM API 速率限制或额度超限" | 稍后重试或换 API Key |
| 认证失败 | "LLM API 认证失败" | 检查 API Key 是否正确 |
| 超时 | "仿真运行超时" | 检查模型配置或网格是否过大 |

### 取消任务

正在排队或运行中的任务，可以点击「取消」按钮终止。

---

## 7. 查看仿真结果

任务完成后（包括失败和取消的任务，如果有部分结果），你可以：

### 7.1 浏览文件

点击「浏览文件」按钮，打开文件浏览器：

- **目录导航**：展开/折叠文件夹，查看完整的文件树结构
- **文本预览**：点击文件名即可在浏览器中查看内容（如 controlDict、fvSchemes、log 文件等）
- **复制内容**：一键复制文件内容到剪贴板
- **单文件下载**：下载单个文件

> **提示**：生成的 OpenFOAM 文件基于 **OpenFOAM v10 (Foundation 版)**。如果你使用 ESI 版本的 OpenFOAM，部分语法可能需要调整。

### 7.2 下载 ZIP

点击「下载结果 (.zip)」可一次性下载所有仿真文件的压缩包。

---

## 8. 提供反馈

你的反馈对我们改进平台非常重要！

### 8.1 文件级反馈

在文件浏览器中，每个文件旁边有一个反馈按钮（💬）：

1. 点击反馈按钮
2. 输入你对该文件的评价（如"边界条件设置有误"、"网格密度不够"等）
3. 提交保存

每条反馈最多 5 KB。已提交的反馈会显示 ✓ 标记。

### 8.2 整体评价

在文件浏览器底部，可以对整个任务进行评价：

| 评分 | 含义 |
|------|------|
| **成功** | 仿真结果正确，满足需求 |
| **部分成功** | 部分正确，但有需要改进的地方 |
| **失败** | 结果不可用 |

可以附加文字评论（最多 500 字）。

### 8.3 交互模式的分阶段评价

如果使用交互模式，每个检查点都可以单独评价：
- 文件生成阶段的评价
- Pre-Run 阶段的评价
- 最终结果的评价

---

## 9. 下载数据

| 下载方式 | 说明 |
|---------|------|
| **ZIP 压缩包** | 点击「下载结果 (.zip)」，包含所有输出文件 |
| **单个文件** | 在文件浏览器中，点击单个文件的下载按钮 |

---

## 10. 使用限制与配额

### 10.1 任务限制

| 项目 | 限制 | 说明 |
|------|------|------|
| 每日任务数（免费模型） | **10 次/天** | UTC 零点重置。BYOK 模式不受此限制 |
| 仿真超时 | **60 分钟** | 超时自动标记为失败 |
| 提交频率 | **5 次/分钟** | 同一 IP 地址 |

### 10.2 存储限制

| 项目 | 限制 |
|------|------|
| 每用户总存储 | **2 GB** |
| 已完成任务保留 | **14 天**后自动删除 |
| 失败/取消任务保留 | **7 天**后自动删除 |

> **提示**：页面右上角会显示你的已用云端存储量。如果存储空间不足，可以手动删除旧任务来释放空间。每个任务卡片右上角都有「删除」按钮。删除后有 8 秒的撤销窗口。

### 10.3 免费模型

| 模型 | 费用 | 限制 |
|------|------|------|
| Codex (gpt-5.3-codex) | 免费 | 每人每天 5 次 |
| Codex (gpt-5.3-codex) | 免费 | 全平台共享额度 |

BYOK 模式下的模型费用由用户自行承担。

---

## 11. 常见问题

### Q: 任务一直显示 "queued"，怎么办？

系统会显示你的排队位置和等待时长。Worker 数量有限，请耐心等待。如果等待超过 30 分钟且排队位置没变，可能是 Worker 离线，请联系管理员。

### Q: 我的 API Key 安全吗？

是的。你的 API Key 仅用于当前任务。Worker 读取后会**立即从数据库中删除**。日志文件上传前也会自动脱敏处理（API Key 会被替换为 `[REDACTED]`）。

### Q: 可以同时提交多个任务吗？

可以。多个任务会按提交顺序排队处理。你可以在提交前查看当前队列状态。

### Q: 仿真失败了，我还能看到文件吗？

可以。失败和取消的任务也会上传已生成的部分文件（如果有的话），你可以通过「浏览文件」查看日志和中间产物。

### Q: OpenFOAM 版本兼容性？

平台使用 **OpenFOAM v10 (Foundation 版)**。如果你使用 ESI 版本（如 OpenFOAM v2312），部分关键字和语法可能有差异，请注意适配。

### Q: 如何获取邀请码？

请联系平台管理员获取。当前处于内测阶段，邀请码数量有限。

### Q: 设置会保存吗？

是的。你的模型选择、执行模式、检查点偏好等设置会自动保存到浏览器本地，下次打开时自动恢复。

---

## 12. 隐私政策

使用 CFDQandA 平台即表示你同意我们的隐私政策。注册时需勾选隐私政策同意框。

关于你的数据，我们承诺：

- **API Key**：仅用于当前任务，Worker 读取后立即从数据库中删除
- **仿真数据**：存储在 Supabase 云端，已完成任务保留 14 天，失败/取消任务保留 7 天，软删除后 3 天永久清除
- **日志脱敏**：日志文件上传前自动将 API Key 替换为 `[REDACTED]`
- **第三方服务**：平台使用 Supabase（数据库/认证/存储）、Vercel（前端托管）和 LLM API（OpenAI/Anthropic 等），你的 Prompt 会发送至 LLM 提供商用于生成仿真配置

完整隐私政策可在页面底部点击「隐私政策」链接查看。如有疑问或需要行使数据权利（删除账户、导出数据、撤回同意等），请联系管理员。

---

## 联系方式

如有问题或建议，请通过以下方式联系：

- **平台反馈**：登录后点击页面底部的反馈按钮
- **GitHub Issues**：在项目仓库中提交 Issue

---

*本指南最后更新于 2026 年 3 月。平台功能可能持续更新，以实际界面为准。*
